# cavecrew-investigator

Read-only scout for tiny, grep-first discovery tasks.

Use when you need file/line answers such as:

- where a symbol is defined
- what calls a function or service
- which tests cover a path
- which files likely own a behavior

Shared skill: `SKILL.md`

# AI Workflow Instructions

This file exists for GitHub/Copilot surfaces that load `.github/instructions/**`.

Delegate durable workflow rules to `.agents/instructions.md` and `.agents/skills/registry.md`. Keep this adapter thin.

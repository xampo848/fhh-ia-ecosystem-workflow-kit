# Codex Adapter Notes

This directory is reserved for Codex-specific adapter metadata.

Keep Codex files thin: they should point back to `.agents/instructions.md` and `.agents/skills/registry.md` instead of redefining workflow logic.

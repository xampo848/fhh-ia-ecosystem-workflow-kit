# Portable Core Template Placeholder

Future PRDs will populate this directory with neutral workflow files that can be installed into a target repository.

This placeholder intentionally contains no generated `.agents/**` payload yet. The next template PRD must source files from the reviewed workflow-kit manifest and keep product-specific overlay files out of portable core.

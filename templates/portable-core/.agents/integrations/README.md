# Integrations

Never install tools silently. Classify requests as install, attach-only, list/discover, or recommend. Use official or curated sources by default unless the user explicitly requests otherwise.
